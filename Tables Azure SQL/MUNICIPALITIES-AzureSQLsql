CREATE TABLE MUNICIPALITIES 
    ( 
     ID_MUN            INT IDENTITY(1,1)  NOT NULL , 
     ID_PROV           INT  NOT NULL , 
     NAM_MUN           VARCHAR (50) , 
     DATE_OPRTN_INSERT DATETIME2 DEFAULT GETDATE()  NOT NULL 
    ) 
;
ALTER TABLE MUNICIPALITIES 
    ADD CONSTRAINT MUNICIPALITIES_PK PRIMARY KEY (ID_MUN);

ALTER TABLE MUNICIPALITIES 
    ADD CONSTRAINT MUNICIPALITIES_FK1 FOREIGN KEY (ID_PROV) 
    REFERENCES PROVINCE (ID_PROV);


